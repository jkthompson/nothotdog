# This is a tensorflow serving model configuration file
# it is generated from the included notebook file
model_config_list {
  config {
    name: 'nothotdog', # name of the model - requests must refer to this name
    base_path: '/tmp', # where is the model stored
    model_platform: "tensorflow",
    # this is required for tensorflow to serve different model versions
    model_version_policy: {
        all: {}
    }
  }
}
